#
# $Header: $HOME/.zsh/functions/info                    Exp $
#

(( $+LOGGER )) && (( $+facility )) && logger -p $facility -t "$argv"

print -P " %F{green}*%f $argv"

#
# vim:fenc=utf-8:ft=zsh:ci:pi:sts=0:sw=2:ts=2:
#
